# FastAPI PDF Server Requirements
fastapi==0.104.1
uvicorn[standard]==0.24.0
uvloop==0.19.0
aiofiles==23.2.1
pydantic==2.5.0
python-multipart==0.0.6

# OCRFlux dependencies (already in base requirements)
# These should be installed via the main OCRFlux setup
# vllm
# pillow
# pypdf
# torch
# transformers